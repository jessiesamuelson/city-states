  <% if @trade_deal.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@trade_deal.errors.count, "error") %> prohibited this trade_deal from being saved:</h2>

    <ul>
      <% @trade_deal.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= form.label :country_resource_id %><br>
    <%= form.select :country_resource_id, CountryResource.all.collect { |r| [Resource.find(r.resource_id).name + ' from ' + Country.find(r.country_id).name.titleize, r.id]} %>
  </div>
  <div class="field">
    <%= form.label :quantity %><br>
    <%= form.text_field :quantity %>
  </div>
  <div id="calculated-cost" class="field">
    Calculated Cost Should Go Here
  </div>

