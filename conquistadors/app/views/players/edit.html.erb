<h1>Editing Player</h1>

<%= form_for(@player) do |form| %> 
  <%= render form %>
  <div class="actions">
    <%= form.submit "update player" %>
  </div>
<% end %>

<%= link_to 'Show', @player %> |
<%= link_to 'Back', players_path %>
